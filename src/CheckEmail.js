import React from "react";
import ShowDashboard from "./ShowDashboard";
export default function CheckEmail() {
  // let validatedUsers = [];
  // if (validatedUsers.indexOf({props.data}) !== -1)
  if (true) {
    return (
      <div className="CheckEmail">
        Account is valid.
        <ShowDashboard />
      </div>
    );
  }
}
